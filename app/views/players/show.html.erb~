<div id="content">

  <div id="tertiaryContent">
    <h3>Statistics</h3>
    <span id="stats">
    </span>
    <div class="xbg"></div>
  </div>

  <div id="primaryContentContainer">
    <div id="primaryContent">
      <h2> Welcome, <%= "#{@player.first_name}!" %></h2>
      <h3>My Ladders</h3>
      <p>
      <% if @ladders.size == 0 %>
				You have not joined any ladders yet.
      <%else%>
				Click on a ladder to view your statistics for that ladder.
				<ul>
					<% @ladders.each do |l| %>
	  				<li><%= link_to_remote( "#{l.name}", {:update => "right", :url =>{ :action => :show_stats, :ladder_id => l.id }}) %></li>
					<% end %>
				</ul>
      <%end%>
      </p><br>

      <h3>My challenges</h3>
      <table>
      <tr class="rowH">
	<th>Challenger</th>
	<th>Defender</th>
	<th>Ladder name</th>
	<th>Challenge date</th>
	<th>Enter score</th>
	<th>Cancel challenge</th>
      </tr>
      <% @challenges.each do |c| %>
      <tr class="rowA">
	<td><%= "#{c.challenger.first_name} #{c.challenger.last_name}" %></td>
	<td><%= "#{c.defender.first_name} #{c.defender.last_name}" %></td>
	<td><%= "#{c.ladder.name}" %></td>
	<td><%= "#{c.created_at.to_s(:short)}" %></td>
	<td><%= link_to 'Enter score', edit_ladder_challenge_path(c.ladder.id, c.id) %></td>
	<% if c.challenger_id == @current_player.id %>
	  <td><%= link_to 'Cancel challenge', 
	    {:controller => "challenges", :action => "destroy", :method => "delete", :id => c.id} %></td>
			<td><%= link_to "Cancel challenge", ladder_challenge_path(c.ladder.id, c.id), :method => "delete",
			:confirm => "Are you sure you want to cancel this challenge?" %></td>
	<% end %>
      </tr>
      <%end%>
      </table>
    </div>
  </div>

  <div id="secondaryContent">
    <h3> Navigation </h3>
    <ul>
      <li><%= link_to 'Edit info', edit_player_path(@player), :method => "get" %></li>
      <li><%= link_to 'View challenges', player_challenges_path(@player), :method => "get" %></li>
    </ul>
    <div class="xbg"></div>
  </div>
  <div class="clear"></div>

</div>


