Hi <%= "#{@player.first_name}" %>
A new comment has been posted by <%= "#{@commenter.first_name}" %> in ladder <%= "#{@ladder.name}" %>

comment:
<%= "#{@comment}" %>

You can respond to this comment in the comments section of the <%= "#{@ladder.name}" %> ladder.
