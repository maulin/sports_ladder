Hi <%= "#{@challenger.first_name}" %>
This is a confirmation email for the challenge you issued.

Challenger: <%= "#{@challenger.first_name}" %>
Defender: <%= "#{@defender.first_name}" %>
Ladder: <%= "#{@ladder.name}" %>