Hi <%= "#{@challenger.first_name} #{@challenger.last_name}" %>
This is a confirmation email for the challenge you issued.

Challenger: <%= "#{@challenger.first_name} #{@challenger.last_name}" %>
Defender: <%= "#{@defender.first_name} #{@defender.last_name}" %>
Ladder: <%= "#{@ladder.name}" %>
